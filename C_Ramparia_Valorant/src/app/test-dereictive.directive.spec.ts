import { TestDereictiveDirective } from './test-dereictive.directive';

describe('TestDereictiveDirective', () => {
  it('should create an instance', () => {
    const directive = new TestDereictiveDirective();
    expect(directive).toBeTruthy();
  });
});
