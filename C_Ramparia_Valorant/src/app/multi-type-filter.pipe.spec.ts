import { MultiTypeFilterPipe } from './multi-type-filter.pipe';

describe('MultiTypeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MultiTypeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
